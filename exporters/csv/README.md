# CSV Exporter

This package will provide CSV output support for GenReport results.

## Purpose

The CSV exporter should transform normalized runtime output into CSV for download or processing.

## Boundary

The exporter only handles output format. It must not own business logic or schema rules.

