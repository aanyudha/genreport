# Excel Exporter

This package will provide Excel output support for GenReport results.

## Purpose

The Excel exporter should transform normalized runtime output into spreadsheet-friendly output.

## Boundary

The exporter only handles output format. It must not own business logic or schema rules.

