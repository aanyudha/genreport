# HTML Exporter

This package will provide HTML output support for GenReport results.

## Purpose

The HTML exporter should transform normalized runtime output into HTML for preview or delivery.

## Boundary

The exporter only handles output format. It must not own business logic or schema rules.

