# PDF Exporter

This package will provide PDF output support for GenReport results.

## Purpose

The PDF exporter should transform normalized runtime output into printable or shareable documents.

## Boundary

The exporter only handles output format. It must not own business logic or schema rules.

